#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef struct ponto_t {
  int x, y;
} ponto_t;

int convexhull(ponto_t pontos, int tamanho){
return 1;
return 0;
}
int main() {
  int qnt;
  scanf("%d", &qnt);
  ponto_t Pontos[qnt];
  for (int i = 0; i < qnt; i++) {
    scanf("%d %d", &Pontos[i].x, &Pontos[i].y);
  }

  return 0;
}